name: Deploy to EC2

on: 
  workflow_call:
   inputs:
    environment:
          description: 'Where to deploy (e.g., staging, production)'
          required: true
          default: 'staging'
  # workflow_run:
  #   workflows: ["Build and Test"]  
  #   types:
  #     - completed  
jobs:
  deploy:
    name: Deploy to EC2
    runs-on: ubuntu-latest
    # needs: [build, test]

    steps:
      - name: Test EC2
        run: |
          echo "Deploying to EC2..."
