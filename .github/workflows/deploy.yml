name: Deploy to EC2

on:
 push:
    branches:
      - develop
    workflow_run:
      workflows: ["Build and Test"]  
      types:
        - completed  

jobs:
  deploy:
    name: Deploy to EC2
    runs-on: ubuntu-latest

    steps:
      - name: Test EC2
        run: |
          echo "Deploying to EC2..."
